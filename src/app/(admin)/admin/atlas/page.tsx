import { AdminPlaceholder } from "@/components/site/admin-placeholder";

/** Atlas authoring lands in a later phase; stub keeps the nav link live. */
export default function AdminAtlasPage() {
  return <AdminPlaceholder title="Atlas" />;
}
